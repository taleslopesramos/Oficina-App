import { StatusBar } from 'react-native'

// Deixa a status bar da mesma cor do header no Android
StatusBar.setBackgroundColor('#2196F3');
// Deixa a cor da status bar branca no IOS
StatusBar.setBarStyle('light-content');
